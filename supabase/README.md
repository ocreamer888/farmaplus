# Supabase (FarmaPlus)

- **migrations/** — versioned SQL applied to Postgres.
- **functions/** — Edge Functions (Deno).

After installing the [Supabase CLI](https://supabase.com/docs/guides/cli), run `supabase init` from the repo root to generate `config.toml` and link a project.
